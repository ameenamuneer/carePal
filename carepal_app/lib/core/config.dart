// ── Server configuration ──────────────────────────────────────────────────
// Change the host here to switch between local, ngrok, and production.
const String _kHost = 'valerie-nondesigned-clement.ngrok-free.dev';

const String kBaseUrl = 'https://$_kHost';
const String kBaseWsUrl = 'wss://$_kHost';
